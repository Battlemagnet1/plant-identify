# plant Identify — 开发计划与可行性分析

| 项 | 内容 |
|---|---|
| 文档版本 | **v1.3** —— v1.0 生成于 2026-09-18；同日据 5 项决策更新为 v1.1；再据 Phase 1–3 交付更新为 v1.2；据 Phase 4 交付更新为 v1.3 |
| 生成日期 | 2026-09-18（Phase 4 更新于 2026-09-19） |
| 分析对象 | `Plant Identify.md`（需求规格书，1730 行） |
| 分析范围 | 需求解构、技术可行性、AI 能力可行性、排期计划、数据架构、风险登记、成本、结论 |
| **决策状态** | **5 项待确认决策已全部确认**，规格书已同步修订（含修订记录），依据见 Part 9 |
| **交付进度** | Phase 1 工程骨架 ✅ ｜ Phase 2 图像采集 ✅ ｜ Phase 3 视觉识别 ✅（Part 4.5.1）｜ **Phase 4 文字分析 ✅**（验证结果见 Part 4.6.1）｜ Phase 5–7 待开工 |
| 结论 | **有条件可行** —— 技术上可落地，但核心卖点「多图联合识别」存在未经验证的退化风险，须先做验证实验 |

### 证据标注约定

本文所有关键数字分为三类，请注意区分：

- **[实测]** — 在本机或官方核查中直接获得的事实
- **[核查]** — 来自官方文档 / 权威来源，文末附来源
- **[估算]** — 本文推算的量级，非精确值，需实测校正

---

## Part 0 执行摘要

### 0.1 一句话结论

**技术上可行，可以开工；但必须先花很小的代价验证一件事 —— 「多图联合识别」这个核心卖点是否真的成立。**

### 0.2 三个关键判断

**① 工程可行性：高。**
本机已具备完整的命令行构建链路（JDK 17.0.20 + Android SDK android-36 + Gradle 9.3.1），且已找到一套**零额外下载**即可编译出 APK 的版本组合。无需安装 Android Studio，无需 JDK 21。缺失的只有模拟器，不影响出包，也恰好——相机、定位这类核心功能本来也只能在真机上验证。

**② 成本可行性：高。**
开发侧零现金成本（工具链全免费）。运行侧单株识别（1 次视觉 + 1 次文本）的费用量级在**分位到角位**，个人使用与教学场景完全可控。

**③ 核心卖点可行性：待验证 —— 这是唯一的高风险项。**
规格书把「多图联合识别，而不是单图识别」定为项目核心特色。但多篇视觉语言模型评测一致显示：**多图输入会显著退化**，模型倾向于把多图当成逐图任务分别描述，跨图综合能力普遍薄弱（跨图综合得分从 10.97 到 56.34 不等，多数模型偏低）。这意味着产品前提**在原理上成立、在工程上不可靠**。

风险不在于"做不出来"，而在于**花 7 个 Phase 做完才发现卖点是空的**。因此本报告的核心建议是：**把验证实验前置到 Phase 3 之前**，用 20–30 组真实样本、几十元的成本，换取整个项目方向的确定性。

### 0.3 关键行动

| 优先级 | 行动 | 状态 |
|---|---|---|
| ~~P0~~ | ~~澄清规格书中的术语与统计口径矛盾（见 Part 1.3）~~ | **✅ 已完成** —— 5 项决策已确认，规格书已修订 |
| ~~P1~~ | ~~确认 MVP 边界（见 Part 9）~~ | **✅ 已完成** —— 第一版含 Phase 6 全做 |
| **P0** | **执行多图识别验证实验**（见 Part 3.4）—— 这是当前唯一未解的高风险项 | 待执行，成本数元至数十元、1–2 天 |
| P2 | 从 Phase 1 工程骨架开始实现 | 待验证实验并行启动 |

> **当前唯一的前置条件就是验证实验。** 术语与 MVP 边界已澄清，可以开工；Phase 1/2 与验证实验可并行，实验结论须在 Phase 3 开工前产出。

---

## Part 1 需求解构

### 1.1 产品定位

| 维度 | 内容 |
|---|---|
| 形态 | Android 原生应用（Kotlin + Jetpack Compose） |
| 核心流程 | 拍照/相册 → 1–5 张照片 → AI 多图联合分析 → 识别 → 植物专业信息 → 建立数字档案 → 搜索/查看/编辑/再观察 → 统计 → 导出 HTML 报告 |
| 目标用户 | 园林专业学生、园林园艺从业者、植物学学习者、植物调查人员、林业生态人员、植物爱好者 |
| 与竞品差异 | 不做「拍照识花」单图工具，做「多图联合识别 + 植物数字档案 + 调查记录」 |

规格书明确要求：不宣称 AI「100% 准确」「保证鉴定正确」「专业鉴定结果」；置信度须说明为「AI 对当前视觉证据的置信程度估计」，而非科学实验验证的物种鉴定概率。**这是一条应当严格执行的产品伦理红线。**

### 1.2 功能分层

规格书要求「不要一次性生成整个项目」，同时又要求「最终得到真正可以安装运行的 APK」。将全部功能按必要性分三层：

#### MVP（Phase 1–5，必须完成）

| 模块 | 功能 |
|---|---|
| 工程基础 | Compose + Material 3、Navigation、Room 三表、依赖注入与架构分层 |
| 图像采集 | CameraX 拍照、系统相册选择（Photo Picker）、1–5 张多图管理、自定义排序、删除、替换 |
| 图像处理 | 原图保存 + AI 上传副本生成（缩放/压缩），原图不可破坏 |
| 视觉识别 | VisionProvider、OpenAI Compatible 多图请求、JSON 解析与容错、错误分类提示、API 配置页与连接测试 |
| 文字分析 | TextProvider、植物简介/形态/习性/花期/果期/用途/养护，**文字分析失败不得丢失识别结果** |
| 结果呈现 | 识别结果页、置信度、识别质量星级、低置信度提示、补图后重新联合识别 |
| 档案管理 | 植物档案 CRUD、多次观察、观察详情、编辑、删除、搜索、筛选、重复植物归并提示 |

#### 增强（Phase 6）

| 模块 | 功能 |
|---|---|
| 统计 | 不同植物数、观察次数、照片数、科数、属数（口径见 Part 1.3 矛盾 2） |
| 位置 | 可选定位、经纬度保存、反向地理编码显示地名、拒绝授权仍可正常使用 |
| 导出 | HTML 导出（单文件 base64 / HTML+图片文件夹 两模式） |
| 备份 | 数据备份与恢复（档案 + 观察 + AI 结果 + 本地图片 + 备注） |

#### 远期（规格书第三十二节，第一版不做）

植物地图、植物分类统计、校园植物调查、植物调查路线、植物群落调查、PDF 报告、Excel/CSV 导出、本地视觉模型、Pl@ntNet 集成、多人共享植物库。

> 架构只需为这些留出扩展空间，不需要在第一版实现。**"为将来留空间"与"现在就实现"是两件事**，规格书自己也强调了「优先保证 MVP 稳定」。

### 1.3 规格书内部矛盾与缺口（第 1、2 项已确认修订）

以下 8 项是在逐条比对规格书中发现的**真实存在的问题**，会直接导致实现歧义。

**其中第 1、2 项已于 2026-09-18 经用户确认并落地到规格书**（规格书开头已加「修订记录」）。此处保留原始分析以说明修订理由；第 3–8 项的处理见 Part 9 决策与 Part 6 风险登记册。

#### ✅ 矛盾 1：表名与类名疑似拼写错误，且大小写不统一 —— **已确认修订**

规格书第十四节使用 `planttRecord`、`planttObservation`（**双 t**），图片表却用大驼峰 `ObservationImage`；第二十七节又出现 `AddplanttScreen`、`planttDetailScreen`（首字母大小写不一致）。

同一份文档中三种命名风格混用。若不修正，将导致 Kotlin 类名、Room 实体名、DAO 名全线不一致，后期改名成本高。

**✅ 用户决策（2026-09-18）**：统一为 `PlantRecord` / `PlantObservation` / `ObservationImage`，页面为 `AddPlantScreen` / `PlantDetailScreen`。规格书全文 **20 处标识符已修订**，并在文档开头加了「修订记录」。

#### ✅ 矛盾 2：统计口径自相矛盾 —— **已确认修订**

规格书首页显示：

```text
植物记录：127
不同植物：83
```

第二十节显示：

```text
植物记录：127
不同植物：83
观察次数：156
```

而第十四点五节定义：`planttRecord` = 一种长期保存的植物档案，`planttObservation` = 某一次观察。

按此定义推理：

- 若「植物记录」指 `PlantRecord` 行数，则它应等于「不同植物」数 → 但 127 ≠ 83，**矛盾**
- 若「植物记录」指观察次数，则它应等于「观察次数」→ 但 127 ≠ 156，**同样矛盾**

三处数字无法同时自洽。若不澄清，首页卡片、统计页、HTML 报告的取数逻辑都会错。

**✅ 用户决策（2026-09-18）：弃用「植物记录」这一含混表述，统一为三项指标**：

| 指标 | 定义 | 示例值 |
|---|---|---|
| 不同植物 | `PlantRecord` 行数 | 83 |
| 观察次数 | `PlantObservation` 行数 | 156 |
| 照片数 | `ObservationImage` 行数 | 247（示例值） |

原来的「植物记录 127」这一数字**作废** —— 它与 83、156 都无法自洽。规格书第十五节首页、第二十节统计页、第二十一节 HTML 报告模板均已同步修订。

> 注意：三项指标的分母语义不同。「不同植物」是去重后的物种数，「观察次数」是累计记录数，「照片数」是图片文件数。UI 上必须并列展示且标注清楚，避免用户误读为同一维度的计数。

#### ⚠ 缺口 3：HTML 单文件导出存在体积爆炸风险

规格书第二十一节要求优先生成单文件 HTML，图片以 Base64/Data URI 嵌入。

**测算**：按「不同植物 83 种、平均每株 2 张原图」估算，单张原图 2–4 MB，Base64 膨胀约 33%：

```
83 种 × 2 张 × 3 MB × 1.33 ≈ 660 MB
```

一个 660 MB 的 HTML 文件在移动端浏览器中**几乎无法打开**。规格书虽提到「图片数量过多时可提供两种模式」，但未给出触发阈值与降级规则。

**建议三级导出策略**：

| 模式 | 触发条件 | 内容 |
|---|---|---|
| 单文件（缩略图） | 默认 | 嵌入 1024px / JPEG 75 缩略图，总体积控制在 50 MB 内 |
| 单文件（原图） | 用户显式选择 | 全部原图 Base64 嵌入，提示文件可能很大 |
| HTML + 图片文件夹 | 照片数 > 200 张 或 预估体积 > 150 MB | HTML 引用相对路径图片，打包为 zip |

#### ⚠ 缺口 4：原图存储位置未定义

规格书只说「保存原图到本地」，未说明存哪里。这个选择有实际后果：

| 方案 | 优点 | 缺点 |
|---|---|---|
| 应用私有目录 `filesDir` | 不占系统相册、卸载即清理、无需权限 | 用户重装即丢失；无法通过相册查看 |
| 系统媒体库 MediaStore | 用户可在相册看到、重装不丢 | 污染用户相册；需 API 权限处理 |
| `cacheDir` | 方便 | **❌ 系统会主动清理，绝对不可用于长期档案** |

**建议**：默认存应用私有目录 `filesDir/images/`，并在设置中提供「同时保存到系统相册」开关。**严禁使用 `cacheDir` 存放档案图片** —— 系统在存储紧张时会静默删除，导致用户档案图片全部丢失。

同时建议数据库中存**相对路径**（如 `images/2026/09/xxx.jpg`）而非绝对路径。Android 应用私有目录的绝对路径包含随机化的应用沙盒路径，换机恢复备份后绝对路径会全部失效。

#### ⚠ 缺口 5：反向地理编码在中国境内的可行路径未定义

规格书第十八节要求「如果条件允许，可以进行反向地理编码」，显示「📍 某校园」而非经纬度。

但反向地理编码需要地理服务。**Google 的地理编码服务在中国大陆无法正常使用**，规格书未给出替代方案。

**建议**：
1. 首选 Android 系统 `Geocoder` —— 但它是**委托给设备厂商实现**的，国内厂商 ROM 多使用高德/百度，可用性因机而异，**必须做失败降级**
2. 备选：接入国内地理服务（高德/腾讯地图）—— 需申请 Key，引入新的密钥管理需求
3. **降级策略（必须有）**：地理编码失败时显示经纬度或「未知位置」，且不阻塞任何主流程。位置本身是可选功能，失败不能影响识别与档案

#### ⚠ 缺口 6：「AI 语义匹配」用于植物归并，与离线/成本目标冲突

规格书第十四点五节的匹配优先级第 4 项是「AI 语义匹配」，用于判断两次识别是否为同一植物。

**问题**：语义匹配需要 embedding 模型调用，这意味着：

- 每次新识别都要额外一次网络请求（成本、延迟）
- 与第二十三节「已保存档案必须能离线查看/编辑」的目标无直接冲突，但增加网络依赖面
- 效果不确定性高：植物同物异名、别名、地方名极多（如「紫薇」与「痒痒树」），embedding 未必优于规则匹配

**建议**：**第一版不做 AI 语义匹配**，仅用规则匹配（见 Part 5.3）。规则匹配已能覆盖绝大部分真实场景，且完全离线、零成本、结果可解释。待用户反馈出现明显误判后，再评估是否引入。

#### ⚠ 缺口 7：API Key 存储方案表述不一

规格书第二十五节同时提到「使用 Android Keystore 等安全方案保护」与「如果使用 DataStore 保存配置，需要考虑敏感信息的加密」，但未给出确定方案。

**技术现状**：`EncryptedSharedPreferences`（androidx.security）已停止维护并被弃用，不应作为新项目方案。

**建议双层方案**：

| 场景 | 存储位置 | 说明 |
|---|---|---|
| 开发期注入的 Key | `local.properties` | 已 gitignore，仅存在于本机，不进版本库 |
| 用户运行时填写的 Key | DataStore（Preferences）+ Android Keystore 加密 | Keystore 生成 AES 密钥并硬件保护，密钥本身不出安全区 |

同时满足规格书第二十五节全部要求：不硬编码、不进 Git、日志不打印、UI 默认隐藏、不上传开发者服务器。

#### 💡 改进建议 8（原创）：给每张图片标注"角色"，而非只排序

规格书允许用户「调整图片顺序」，但没有让用户**标注每张图的拍摄部位**。

这一点与 Part 3 的核心风险直接相关：多图识别的退化，很大程度上源于模型不知道"每张图该看什么"。规格书的 prompt 里虽然列出了"整体株型/叶片/花/果实/茎干/树皮"等观察重点，但**模型只能自己猜哪张图对应哪个部位** —— 而它猜错时，跨图综合就变成了错误的信息拼接。

**建议**：`ObservationImage` 增加 `role` 字段，用户在添加图片时可选标注：

```
WHOLE_PLANT（整株）｜ LEAF（叶片）｜ FLOWER（花）
FRUIT（果实）｜ BARK（茎干/树皮）｜ HABITAT（生境）｜ UNKNOWN（未标注）
```

prompt 中改为显式告知：`照片1（整株）：…  照片2（叶片）：…`。

**这是一个低成本、高潜在收益的改动**：UI 上只是每张缩略图下方加一个标签选择器，不增加用户负担（默认 `UNKNOWN`），但可能显著改善多图联合识别的效果。它也正是 Part 3.4 验证实验中 B 组要验证的假设。

### 1.4 必须严格遵守的产品伦理红线

规格书第三十一节已明确，此处再确认三条：

1. **不宣称准确率**。UI 中禁止出现「91% 一定正确」这类表述
2. **置信度不是概率**。须说明为「AI 对当前视觉证据的置信程度估计」
3. **识别质量星级不是科学评级**。它是应用根据置信度与图片完整度生成的辅助提示

---

## Part 2 技术可行性

### 2.1 构建环境实测结果

#### 已具备 [实测]

| 项 | 实际情况 | 位置 |
|---|---|---|
| Git | 2.54.0.windows.1；全局身份 `Battlemagnet1` | 已在 PATH |
| GitHub CLI | 2.97.0，已登录 Battlemagnet1（scopes: `gist, read:org, repo`） | `C:\Program Files\GitHub CLI\gh` |
| JDK | **17.0.20** | `C:/Users/a/jdk/jdk17.0.20_8` —— **不在 PATH，须显式指定 `JAVA_HOME`** |
| Android SDK | 2.9 GB，含 platforms 35/36、build-tools 35.0.0/36.0.0、cmdline-tools/latest、platform-tools(adb)、ndk、cmake，**licenses 已接受** | `C:/Users/a/Android/Sdk` |
| Gradle | wrapper 发行版已缓存 8.10.2 与 9.3.1 | `~/.gradle/wrapper/dists` |
| Maven 镜像 | 全局 `~/.gradle/init.gradle` 已配阿里云镜像（google/central/gradle-plugin/public） | 无需额外配置，国内拉依赖顺畅 |
| debug.keystore | 已存在 | `~/.android/debug.keystore` |

**结论**：命令行构建链路完整，**今天就具备产出 debug APK 的能力**。

#### 缺失项 [实测]

| 缺失 | 影响 | 应对 |
|---|---|---|
| **无 emulator 包、无 AVD** | 无法在电脑上可视化运行 | 用真机 + adb；如需模拟器，后续安装 emulator + 系统镜像（约 8–10 GB） |
| **无 Android Studio** | 无法用 IDE 的可视化编辑器与 Inspector | 全程命令行构建；代码仍可在 VS Code 中编辑；交付物依然是标准 Android Studio 工程，用户可随时用 Studio 打开 |
| 磁盘 C 剩 30 GB / D 剩 35 GB | 够用但不宽裕 | Gradle 缓存与构建产物集中管理，及时 `gradlew clean` |

> **注意**：规格书第三十五章要求交付「完整 Android Studio 项目」。这一点不受影响 —— **项目本身就是标准 Android Studio 工程**，只是本机的构建与验证走命令行。用户随时可以安装 Studio 打开它。

### 2.2 工具链版本矩阵（零下载组合）[核查]

| 项 | 版本 | 关键约束 |
|---|---|---|
| JDK | 17.0.20 | AGP 9.x 的最低与默认 JDK 都是 17，**不需要 JDK 21** |
| Gradle | 9.3.1 | AGP 9.1.x 要求最低 Gradle 9.3.1，与本机缓存精确匹配 |
| AGP | **9.1.1** | 本机 Gradle 9.3.1 能支撑的最高 AGP 版本。⚠ AGP 9 起**内置 Kotlin**，不能再手动应用 `org.jetbrains.kotlin.android` |
| Kotlin | **2.2.10**（AGP 内置，不可自选） | ⚠ **原估的 2.4.20 不可用**：AGP 9.1.1 的 POM 锁定 `kotlin-gradle-plugin:2.2.10`。Compose 编译器插件版本必须与之相同 |
| Compose BOM | **2026.06.00** | 映射 Compose UI 1.11.3 / Material3 1.4.0，配合 compileSdk 36 |
| Compose 编译器插件 | 2.2.10 | 仍需显式应用（`org.jetbrains.kotlin.plugin.compose`）；**版本号必须 = Kotlin 版本号**，否则直接报版本冲突 |
| KSP | **2.3.12** | ⚠ **必须用新版独立版本号**。与 Kotlin 版本号对齐的 `2.2.10-2.0.2` 反而不可用 —— 它用旧的 `kotlin.sourceSets` 注册生成目录，与 AGP 9 内置 Kotlin 冲突 |
| Room | 2.8.5 | 仍需 KSP 注解处理 |
| CameraX | **1.6.2** | ⚠ **不要用 1.6.0**（`ListenableFuture` 编译错误）。原估的 1.6.1 偏旧，实际最新稳定版为 1.6.2 |
| Retrofit / OkHttp | 3.0.0 / 5.5.0 | 使用 `okhttp-bom` 统一版本；Retrofit 3.x 对 2.x 前向二进制兼容 |
| DataStore | 1.2.1 | `datastore-preferences` |
| navigation-compose | 2.10.1 | |
| activity-compose | 1.13.0 | |
| lifecycle | 2.10.0 | `lifecycle-runtime-compose` 提供 `collectAsStateWithLifecycle` |
| core-ktx | 1.17.0 | 更新的 1.19.0 可能要求 compileSdk 37，暂不采用 |
| compileSdk / targetSdk | **36 / 36** | Google Play 自 2026-08-31 起要求新应用 targetSdk ≥ 36；本机已装 android-36 |
| minSdk | **26** | 受测试机限制：模拟器为 Android 14 / API 34，minSdk 必须 ≤ 34 |

> **上表已由实际构建验证**（2026-09-18，AGP 9.1.1 工程 `assembleDebug` 通过）。
> 原始调研中有三处与实测不符：Kotlin 版本、KSP 版本策略、CameraX 最新版本。
> 三者的实测结论分别是：Kotlin 由 AGP 锁定为 2.2.10；KSP 必须用新版独立版本号而非与 Kotlin 对齐的旧式版本号；
> CameraX 最新稳定版为 1.6.2。

#### 为什么不用最新版本（技术债说明）

这不是能力限制，而是**主动取舍**，须记录在案：

| 组件 | 最新稳定版 | 未采用的原因 | 补齐条件 |
|---|---|---|---|
| AGP | 9.4.0 | 要求 Gradle ≥ 9.6.0，本机未缓存 | 下载 Gradle 9.6 |
| Compose BOM | 2026.09.00 | 强制 compileSdk **37** 且 AGP ≥ 9.2 | 安装 android-37 平台 + 升级 AGP |
| Kotlin | 2.4.20 | 受 AGP 9.1.1 内置的 KGP 2.2.10 限制，**在当前 AGP 下无法使用** | 需绕开内置 Kotlin（在 buildscript 显式声明 classpath 强升），代价与风险都不划算 |

**建议**：Phase 1 先用零下载组合跑通，确认整条链路畅通；Phase 7 做 Release 构建时再评估是否升级。**不要在项目初期同时引入构建系统升级的不确定性**。

### 2.3 逐组件可行性评估

| 组件 | 可行性 | 验证方式 | 说明 |
|---|---|---|---|
| Kotlin + Compose + Material 3 | 高 | 编译 + 真机 | 规格书第二十六节的 UI 要求（圆角卡片、留白、植物主题色、避免过度装饰）完全可用 Material 3 实现 |
| Navigation Compose | 高 | 编译 + 真机 | 规格书第二十七节的 7 个页面结构可用单 Activity + Compose Navigation 实现 |
| Room 三表 + 关系 | 高 | 编译 + 单元测试 | 一对多关系与级联删除用 `@Relation` + `ForeignKey` 实现；**必须开启 `exportSchema`** |
| CameraX | 中高 | **必须真机** | 拍照无法在无相机设备上验证；权限与生命周期处理是主要复杂度 |
| Photo Picker | 高 | 真机 | `ActivityResultContracts.PickVisualMedia` **无需任何存储权限**，是 targetSdk 36 下的推荐做法，规格书未提及这个简化机会 |
| Retrofit + OkHttp 多图上传 | 中高 | 可离线构造 + 真机 | 难点在请求体积（见 Part 3.3）与超时配置，非接口本身 |
| DataStore + Keystore | 中高 | 真机 | Keystore 在模拟器上可跑，但硬件保护特性需真机验证；逻辑本身清晰 |
| 图片压缩与存储 | 中 | 真机 | 需处理 HEIC/超大图/旋转 EXIF 等边界 |
| **HTML 导出（单文件 base64）** | **中** | 可离线验证 | **主要难点是体积，见 Part 1.3 缺口 3；Base64 编码在 Android 端用 `android.util.Base64` 或 Okio，无技术障碍** |
| 反向地理编码 | **中低** | 真机 | **Google 服务国内不可用，须做降级，见 Part 1.3 缺口 5** |
| 数据备份/恢复 | 中高 | 可离线验证 | 建议用 zip 打包（SQLite 文件 + 图片目录 + manifest.json） |

### 2.4 架构可行性

规格书第二十八、二十九节给出的架构与 Provider 设计是合理且可落地的：

```text
UI (Compose)
    ↓
ViewModel
    ↓
UseCase
    ↓
Repository
    ↓
├── Room（本地数据库）
├── File Storage（图片文件）
└── AIProvider（远程 AI 服务）
```

**AI Provider 的关键设计判断**（规格书第二十九节已给出正确方向）：虽然文档列出了 `QwenVisionProvider`、`GLMVisionProvider`、`DeepSeekProvider` 等多个实现类，但又指出「如果多个服务都遵循 OpenAI API，可以优先统一为 `OpenAICompatibleProvider`」。

**这个判断是对的，且应当贯彻得更彻底**：Qwen-VL（DashScope 兼容模式）、豆包（火山方舟）、GLM-4V、OpenAI 全都提供 OpenAI 兼容的 `/chat/completions` 端点。因此：

- **接口层**：只定义 `VisionProvider` 与 `TextProvider` 两个接口
- **实现层**：只实现一个 `OpenAICompatibleVisionProvider` 与一个 `OpenAICompatibleTextProvider`
- **差异处理**：把各家的差异（base_url 默认值、多图上限、是否需要 data URI 前缀、参数名）**抽成配置项**，而不是为每家写一个类

这样代码量减少数倍，新增服务商只需加配置。规格书要求的「可扩展性」由配置实现，而非由继承实现。

---

## Part 3 AI 能力可行性（核心章节）

> 本章是全部可行性的关键。如果这一章的问题不解决，其余部分做得再好也没有意义。

### 3.1 各家视觉模型的多图支持情况 [核查]

| 服务 | 多图同请求 | Base64 内联 | 单图限制 | 备注 |
|---|---|---|---|---|
| **Qwen-VL**（DashScope 兼容模式） | ✅ 可靠支持，官方示例即两张图一次请求 | ✅ | ≤ 10 MB（新系列 20 MB）；建议长边 ≤ 8K；长宽比 ≤ 200:1 | base64 最多 250 张；data URI 字符串 ≤ 20 MB |
| **豆包 Doubao-vision**（火山方舟） | ✅ 支持，无硬性张数上限（受上下文限制） | ✅ | ≤ 10 MB；base64 请求体 ≤ 64 MB；支持 `detail: low/high` | 社区建议单请求 5–10 张 |
| **OpenAI GPT-4o / 4.1** | ✅ 支持 | ✅ | ≤ 20 MB | 官方未公开硬性张数上限 |
| **GLM-4V 系列** | ⚠️ **不确定** | ⚠️ **有坑** | 二手来源称 ≤ 5 MB / ≤ 6000×6000，**来源相互冲突** | 官方文档页当前 404；**GLM-4V-Flash 明确不支持 base64**；部分版本需去掉 `data:image/...;base64,` 前缀 |

**结论与建议**：

- **首版官方支持三家**：Qwen-VL、豆包、OpenAI。这三家的多图 + base64 能力有可靠依据
- **GLM 标注为「实验性支持」**：由于文档不可达且社区反馈冲突，不应把 GLM 列入首版的兼容性承诺。UI 上可保留自定义 OpenAI Compatible 入口让用户自行尝试，但文档中须注明风险
- **智谱的坑要特别写入代码注释**：`GLM-4V-Flash` 不支持 base64 且部分版本不认 data URI 前缀 —— 这会导致「配置看起来正确但请求必然失败」，排查成本高

### 3.2 结构化输出的现实约束 [核查]

规格书第五节要求 AI 返回严格的结构化 JSON。现实是：

| 服务 | `response_format: json_object` | `json_schema`（强制 schema） |
|---|---|---|
| OpenAI | ✅ | ✅（最稳） |
| Qwen | 支持面较广 | 仅少数模型；官方列表注明最新版与快照版的 qwen-vl-max **不在支持范围** |
| 豆包 | 有 `response_format` 参数 | 视觉模型无严格 schema 保证 |
| GLM-4V | 宣称原生支持 JSON | 无 schema 强制机制 |

**这意味着一件必须写入架构的事**：**不能依赖 `response_format` 保证 JSON 合法性**，必须实现完整的容错链路：

```text
1. prompt 中给出完整的 JSON 结构示例与字段说明
2. 请求尽量带上 response_format: json_object（支持则带，不支持则忽略）
3. 解析时先剥离可能的 ```json 代码块围栏
4. 解析失败 → 尝试正则提取第一个完整的 {...} 块
5. 仍失败 → 带错误信息重试一次（提示模型"上次输出不是合法 JSON"）
6. 再失败 → 降级为半结构化：展示原始文本 + 提示"结构化解析失败"，但**不丢弃本次识别的文字内容**
```

第 6 步很重要：**解析失败不等于识别失败**。模型可能已经识别对了，只是格式不规范。直接报错让用户重新拍照是糟糕的体验。

此外，无论解析是否成功，都建议**把原始返回完整存入 `PlantObservation.aiResultJson`**，便于问题追溯与后续改进 prompt。

### 3.3 请求体积测算 [估算]

这是决定用户体验的工程参数。规格书第十三节建议「最长边 1536–2048 px，JPEG 质量 75–85」，但未给出理由。实际测算：

| 方案 | 单图体积 | 5 图原始体积 | Base64 后（×1.33） | 移动网络上传耗时 |
|---|---|---|---|---|
| 2048 px / JPEG 85 | 400–800 KB | 2.0–4.0 MB | **2.7–5.3 MB** | 5–20 s |
| **1536 px / JPEG 80** | **250–400 KB** | **1.25–2.0 MB** | **1.7–2.7 MB** | **3–10 s** |
| 1024 px / JPEG 75 | 120–200 KB | 0.6–1.0 MB | 0.8–1.3 MB | 2–5 s |

各家限额都够用（豆包 base64 请求体上限 64 MB，Qwen 单图上限 10 MB），**瓶颈不在 API，而在用户等待时间与流量消耗**。

**建议取 1536 px / JPEG 80**：

- 1536 px 对植物识别而言细节足够 —— 叶片形态、叶序、花部结构在这个分辨率下都清晰可辨
- 5 图总请求体 < 2.7 MB，4G 下约 3–10 秒，用户可接受
- 相比 2048 px 方案，上传耗时减少约一半，费用降低约 40%
- **原图仍完整保存在本地**（规格书第十三节要求），压缩副本仅用于上传

**实现要点**：

- 使用 `BitmapFactory.Options.inSampleSize` 做二次幂降采样，再用 `Bitmap.createScaledBitmap` 精确缩放到目标长边
- **必须处理 EXIF 旋转**：很多手机拍摄的照片靠 EXIF 标记方向，直接压缩会导致图片上下颠倒或旋转 90°，**进而严重影响识别准确率**
- 压缩在后台线程执行（`Dispatchers.Default`），并提供进度提示
- 压缩失败或图片格式异常时，降级为使用原图（同时检查体积是否超限）

### 3.4 ⚠ 核心风险：多图联合识别的退化

**这是本次分析最重要的发现，也是唯一的高风险项。**

#### 学术证据 [核查]

多篇视觉语言模型评测文献一致指向同一个结论：**多图输入的性能显著低于单图**。具体现象：

| 现象 | 说明 |
|---|---|
| 跨图信息泄漏 | 模型把不同图片的内容混淆，错误关联 |
| 「名为多图实为单图」 | 模型实际只深度利用了其中一张图，其余图仅作浅层参考 |
| 注意力转移 | 深层注意力从「跨图」转向「图内」，失去联合推理能力 |
| 信息分散惩罚 | 当判断所需信息分散在多张图时，准确率骤降 |
| 干扰图惩罚 | 无关图片会进一步拉低表现 |

跨图综合（cross-image synthesis）基准得分（分数越高越好）：

| 模型 | 得分 |
|---|---|
| InternVL3.5-8B | 10.97 |
| Phi-4-multimodal | 20.93 |
| Qwen2.5-VL | 56.34 |

**三个模型、最高不到 60 分**，且这是通用基准。植物识别要求更精确的细粒度判别（区分同属近缘种），难度只会更高。

#### 这对本项目意味着什么

规格书的核心设计思想是：

> 照片1：完整植株｜照片2：叶片｜照片3：花朵｜照片4：果实｜照片5：茎干/树皮
> AI 不应该把每张图片当成完全独立的识别任务，应该将它们视为同一株植物的不同观察角度

**这个产品洞察是对的** —— 植物鉴定确实需要综合多器官特征，专业鉴定也确实是这么做的。问题在于**当前模型未必能可靠执行跨图综合**。

因此风险不是「功能做不出来」，而是两种具体后果：

1. **卖点落空**：产品宣称「多图联合识别」，实际效果与「单图识别取最好结果」没有显著差异，用户感知不到价值
2. **更糟的情况**：多图反而**降低**了准确率（引入了错误的信息拼接），比单图还差

#### 但不能因此否定方案 —— 有两个对冲手段

**手段一：显式角色标注（对应 Part 1.3 改进建议 8）。**
模型之所以退化，一个重要原因是它需要自己判断「哪张图是什么部位」。若 prompt 直接告知「照片2 是叶片特写，请重点观察叶形与叶序」，就把一个需要跨图推理的任务，简化成了多个可由单图完成的子任务。**这可能是最有效的改进方向**，因为它绕开了模型的短板。

**手段二：多图 + 单图结论汇总的混合策略。**
先让模型对每张图独立描述观察到的特征（这是模型擅长的），再由同一模型或文本模型基于这些描述做综合判断（这是纯文本推理，模型表现可靠）。这实际上是规格书明确反对的做法（"不要简单地分别识别每张图片然后投票"），但**反对的是"投票"，混合策略做的是"先提取特征、再综合推理"，两者不同**。需要实验验证。

#### 验证实验设计（必须在 Phase 3 之前完成）

这是本报告对项目推进方式的核心建议。

**目标**：用最小成本，确定多图联合识别在真实植物照片上的实际表现，并据此选定 prompt 策略。

**样本**：

- 20–30 组，每组为**同一株植物的 1–5 张照片**（整株/叶片/花/果实/树皮）
- 覆盖四个生活型：乔木、灌木、草本、藤本
- **必须包含 3–5 组易混种对照**，例如：紫薇 vs 大花紫薇、桂花 vs 女贞、月季 vs 玫瑰。易混种才真正考验多器官综合判断能力

**三组对照**：

| 组别 | 方法 | 验证的假设 |
|---|---|---|
| **A 组** | 多图原样单次请求（规格书原始 prompt） | 基线：模型能否自发做跨图综合 |
| **B 组** | 多图 + 每图角色标注单次请求 | 显式角色标注能否显著改善 |
| **C 组** | 逐图独立提取特征 → 文本模型综合判断 | 混合策略是否优于纯多图 |

**基准**：

- **Pl@ntNet** 官方 API（它本身就支持「同一株 1–5 张图片」，与本产品的输入设定完全一致，是理想的对照基准）
- **人工判定**作为最终真值（可由用户本人或专业人士完成）

**指标**：

| 指标 | 说明 |
|---|---|
| Top-1 命中率 | 第一候选是否等于真值 —— **最关键指标** |
| 科/属正确率 | 即使种错了，科属对了说明识别有部分价值 |
| 置信度与实际正确性的相关性 | **若高置信度的样本并不更准，则 UI 上的置信度展示就是误导**，必须重新设计 |
| 延迟 | 端到端耗时可接受性 |
| Token 成本 | 单次识别的实际费用 |

**产出**：一张对比表 + 明确的策略决策（采用 A/B/C 中哪一组作为正式实现）。

**成本** [估算]：约 25 组 × 3 策略 ≈ 75 次视觉调用，按国产视觉模型价格量级，**总费用数元至数十元**。

**投入产出比极高**：用几十元和 1–2 天，换取整个项目方向的确定性。相比之下，如果等功能做完才发现卖点不成立，返工成本是它的几十倍。

**执行方式**：可以写一个纯 Python 脚本跑（不依赖 Android 工程），因此**可以与 Phase 2 并行进行**，不占用开发进度。

#### 若验证结果不理想，怎么办

三种预案，按优先级：

1. **改用 B 组的角色标注策略**（最可能有效，且改动小）
2. **诚实调整产品表述**：不宣称"多图联合识别"，改为"多角度辅助识别"，并在 UI 上如实说明多图的作用是"提供更多可参考的视觉证据"，而非"保证更高准确率"
3. **接入 Pl@ntNet 作为可选通道**：它的 API 本身就按「1–5 张同株图片」设计，可作为独立兜底，与本产品的输入形式天然契合。注意其免费额度口径存在冲突（见 Part 7.2）

**无论哪种预案，都不应该为了保住建言而虚报效果。** 规格书第三十一节自己已经定下了诚信原则，本报告只是把它贯彻到底。

### 3.5 替代方案调研结论

| 方案 | 结论 |
|---|---|
| **Pl@ntNet** | ✅ **可用且高度契合**。官方 REST API 明确支持提交同一株的 1–5 张图片，与产品输入设定完全一致。**✅ 决策：先仅用作验证实验的对照基准**，是否常驻为兜底通道等实验结果再定（见 Part 9）。**注意免费额度口径冲突**（见 Part 7.2） |
| **iNaturalist CV API** | ❌ **实质已封闭**。完整分类模型（约 6 万 taxa）因版权与政策原因不公开，仅对少数机构收费开放；GitHub 上仅公开约 500 taxa 的小模型，不足以支撑产品。**报告中明确排除** |
| 本地视觉模型 | ❌ 第一版不可行。植物细粒度识别的本地模型体积与算力需求超出手机端可接受范围，且与「支持自定义 OpenAI Compatible API」的架构方向相悖 |

---

## Part 4 计划可行性

### 4.1 整体节奏

规格书第三十三节已给出 Phase 1–7 的开发顺序，**这个划分是合理的**，本报告予以沿用，仅做两处调整：

- **调整一**：把「多图识别验证实验」插入为 Phase 3 的前置条件（Phase 0 性质）
- **调整二**：为每个 Phase 补上**可执行的验收标准** —— 规格书反复要求「确保可以编译运行」「检查已有功能是否被破坏」，但没有定义"通过"的客观标准，这会导致阶段推进的判据模糊

### 4.2 依赖关系与并行机会

```text
Phase 1（工程骨架）
    ↓
    ├──────────────────────────┐
    ↓                          ↓
Phase 2（拍照/相册/本地图片）   验证实验（纯脚本，不依赖 Android 工程）
    ↓                          ↓
    └────────────┬─────────────┘
                 ↓
          Phase 3（VisionProvider）   ← 必须等验证实验结论
                 ↓
          Phase 4（TextProvider）
                 ↓
          Phase 5（档案/搜索/归并）
                 ↓
          Phase 6（统计/位置/导出/备份）
                 ↓
          Phase 7（优化/测试/Release）
```

**关键并行点**：验证实验是纯 API 调用脚本，不依赖任何 Android 代码。因此它可以与 Phase 1、Phase 2 **完全并行**，只需在 Phase 3 开工前产出结论即可。**这不会拖慢任何进度** —— 这是把验证前置的关键理由。

### 4.3 Phase 1 —— 工程骨架

| 项 | 内容 |
|---|---|
| **交付物** | Gradle Kotlin DSL 工程 + 版本目录（`libs.versions.toml`）、Compose + Material 3 主题（植物主题色）、Navigation 图（7 个页面占位）、Room 三表 + DAO + 数据库、`local.properties.example` |
| **依赖** | 无 |
| **验收标准** | ① `./gradlew assembleDebug` 零错误产出 APK<br>② `./gradlew lint` 无 error 级问题<br>③ Room schema 已导出至 `app/schemas/`<br>④ 真机安装后可在 7 个页面间跳转<br>⑤ 应用可正常冷启动，无崩溃 |
| **主要风险** | 版本兼容性（AGP/Kotlin/KSP 三者对齐）。**实测已踩到三个坑，均已在工程中规避**：① AGP 9 内置 Kotlin，不能应用 `org.jetbrains.kotlin.android`；② Kotlin 版本被 AGP 锁定为 2.2.10，不可自选；③ KSP 必须用新版独立版本号 2.3.12，与 Kotlin 对齐的旧式 `2.2.10-2.0.2` 会与内置 Kotlin 冲突 |
| **自检命令** | `JAVA_HOME=C:/Users/a/jdk/jdk17.0.20_8 ./gradlew assembleDebug` |

### 4.4 Phase 2 —— 图像采集与本地存储

| 项 | 内容 |
|---|---|
| **交付物** | CameraX 拍照页、Photo Picker 相册选择、多图网格（1–5 张 + 添加入口）、删除/替换/排序、**图片角色标注（改进建议 8）**、压缩流水线（含 EXIF 旋转处理）、原图存 `filesDir` |
| **依赖** | Phase 1 |
| **验收标准** | ① `assembleDebug` 零错误<br>② 真机完成「拍照 → 添加 3 张 → 本地保存 → 杀进程重启 → 数据与图片仍在」<br>③ 删除/排序后顺序持久化正确<br>④ 压缩后长边为 1536 px 且方向正确（无旋转/翻转）<br>⑤ 拒绝相机权限后应用不崩溃，可改用相册 |
| **主要风险** | EXIF 方向处理；超大图（如 108MP）内存溢出 —— 需用 `inSampleSize` 分步降采样；部分 ROM 的 Photo Picker 实现差异 |
| **必须真机** | 是（相机无法在无相机环境验证） |

### 4.5 Phase 3 —— 视觉识别 ✅ 已完成（2026-09-18）

| 项 | 内容 |
|---|---|
| **交付物** | `VisionProvider` 接口 + `OpenAICompatibleVisionProvider` 实现、多图请求体构造、prompt（含角色标注）、容错 JSON 解析链路、错误分类与中文提示、设置页 AI 配置（Base URL / API Key / Model）+ 测试连接按钮、**预置 Qwen / 豆包 / OpenAI 三家默认配置**（决策见 Part 9）、基于验证实验结论的 prompt 策略 |
| **依赖** | Phase 1；**验证实验结论** |
| **验收标准** | ① `assembleDebug` 零错误<br>② 用测试 Key 跑通 1 张与 5 张两组，均返回结构化结果<br>③ 五类错误均给出可理解的中文提示且不崩溃：断网、Key 错误、Base URL 错误、模型不存在、返回非 JSON<br>④ API Key 在 UI 中默认隐藏，且不出现在任何日志中<br>⑤ 解析失败时展示原始文本而非空结果<br>⑥ **切换三家预置配置时，Base URL / Model 正确带出默认值，且用户可完全覆盖**；三家差异全部落在配置文件，切换不需改代码 |
| **主要风险** | 各家 API 差异（base_url 路径、多图上限、data URI 前缀）；结构化输出不稳定；错误分类的完备性 |

#### 实际落地与计划的差异

| 项 | 计划 | 实际 | 原因 |
|---|---|---|---|
| 网络库 | Retrofit 3.0.0 + OkHttp 5.5.0 | **仅 OkHttp 5.5.0** | 只有 1–2 个端点；且容错解析需要「读原始字符串再逐级降级」，序列化框架的严格解析与这个目标冲突 |
| 错误分类 | 五类 | **十二类** | 实测发现若干计划外但常见的错误需要单独提示：明文 HTTP 被拦（`CleartextBlocked`）、**模型不吃图片**（`ModelNotVisionCapable`）、413、TLS、超时等 |
| 验证方式 | 「用测试 Key 跑通」 | **本地模拟服务端 + 19 项自动断言** | 见下方「验证方法」 |
| prompt 策略 | 等验证实验结论 | **A / B 两组做成可切换配置** | 让实验可以直接用 App 跑，不必另写脚本；结论出来后只改一个默认值 |
| 测试连接 | 单次带图请求 | **带图 + 纯文本降级，两轮** | 真实事故：1×1 探针图被百炼的 `min_pixels` 规则拒绝，导致正确配置被报成「连接失败」。详见 4.5.2 |

#### 验证方法：本地模拟服务端

没有真实 API Key 时，验收标准 ②③⑤ 无法稳定复现 —— 特别是「返回非 JSON」「字段名写成 camelCase」这类情形，不该为了测一个错误分支去烧额度。

因此新增 `tools/mock_ai_server.py`：一个可切换响应模式的 OpenAI 兼容服务端，
配合 `adb reverse tcp:8899 tcp:8899` 让设备通过 `127.0.0.1:8899` 访问本机。
它同时记录收到的请求摘要（图片张数、prompt 全文），使「到底发出去了什么」也可被断言。

配套的 `tools/verify_phase3.py` 驱动 UI 完成 19 项断言，结果见 Part 4.5.1。

#### 4.5.1 Phase 3 验证结果

| 组 | 项数 | 结果 |
|---|---|---|
| 服务端错误 → 中文提示（401/404/400模型/400图片/429/500） | 6 | ✅ 全通过 |
| 返回内容容错（标准/围栏/夹带文字/camelCase/字段缺失/非 JSON） | 6 | ✅ 全通过 |
| 字段级容错（camelCase 键名、`"91%"` 字符串、字符串型数组） | 3 | ✅ 全通过 |
| 非 JSON 时的降级行为（提示 + 保留原文 + 如实说明） | 3 | ✅ 全通过 |
| 解析失败后的重试（prompt 带上纠正说明） | 1 | ✅ 通过 |
| 请求内容（3 图 / Authorization / 模型名 / prompt 关键指令） | 7 | ✅ 全通过 |
| API Key 不入日志（明文 / Authorization 头 / 密文 / Base URL） | 4 | ✅ 全通过 |
| 编译与 lint | 2 | ✅ `assembleDebug` 成功；lint **0 error** |

**实测修正的两处错误分类缺陷**（均由验证发现）：

1. 服务端返回 `Invalid content type: image_url is not supported by this text only model`
   时被归类为「模型名不正确」——因为判定规则里含裸的 `model` 一词。
   实际问题是**配成了纯文本模型**，提示应指向「换一个视觉模型」。
   已新增 `ModelNotVisionCapable` 类型，并把「图片被拒」的判断提到「模型名错误」之前。
2. 修正后规则收得过紧，`The model \`xxx\` does not exist` 又不再被识别为「模型不存在」。
   最终采用「具体短语 + 泛化的存在性否定」组合，并依赖判定顺序避免误伤。

这两个缺陷说明：**错误分类的正确性只能靠穷举真实响应文本来保证**，
单靠设计时的直觉一定会漏。

#### 4.5.2 上线后修复：测试连接在真实 API Key 下误报失败

**现象**（用户实测反馈）：用真实 API Key 时**识别完全正常**，但点「测试连接」报
`连接失败 服务返回：<400> InternalError.Algo.InvalidParameter`。

**根因**：测试连接用的探针图是一张 **1×1 像素** 的 PNG。阿里云百炼官方限制：

- 图像「宽度和高度均须大于 10 像素」
- `min_pixels` 下限：qwen-vl-max / qwen-vl-plus 为 **4096**，Qwen3-VL 系列为 **65536**

1×1 两条都不满足。于是「测试连接」对一个**完全正确**的配置报失败，
而实际识别（1536px 照片）又一切正常 —— 用户只能陷入困惑。

**修复（两层，缺一不可）**：

1. **探针图改为运行时生成的 256×256**（= 65536 像素，满足最严格的那一档），
   画简单双色图案而非纯色。用程序生成而不是硬编码 base64，
   便于将来按服务商要求调整（`PROBE_EDGE` 常量）。
   实测 base64 从 114 字节变为 1242 字节。

2. **测试连接增加降级重试**：带图请求若被参数校验拒绝（400/422 且非模型名问题），
   自动改用纯文本重试；纯文本能通就报「连接正常（图片未验证）」而非「连接失败」。

   理由：测试连接的首要目的是验证「地址 / Key / 模型名」，图片支持只是附加项。
   **因附加项失败而否定整体，会让用户去反复核对一个其实正确的配置。**

**通用教训**：

> 一个「体检」功能如果把某个**可选能力**的失败算作整体失败，
> 它就会在配置正确时给出错误结论 —— 这比不提供体检更糟。

**验证**：给模拟服务端加了 `params` 模式（仅带图请求返回 `InvalidParameter`，
纯文本正常）与 `/requests` 端点（返回全部请求记录），据此断言：

| 场景 | 期望 | 结果 |
|---|---|---|
| mode=ok | 「连接正常」，1 次请求 | ✅ |
| mode=params | 「连接正常（图片未验证）」，2 次请求 | ✅ |
| 401 | 仍如实报「API Key 无效」，不降级重试 | ✅ |
| 探针图体积 | 远大于 1×1 的 114 字节 | ✅ 1242 字节 |

同时改进：`extractDetail` 现在会一并提取服务端的 `code` 字段 ——
DashScope 的 message 只有一句 `<400> InternalError.Algo.InvalidParameter`，
光看它无法定位问题，而 code 里的 `InvalidParameter` 至少指明了「参数层面」。

### 4.6 Phase 4 —— 文字分析 ✅ 已完成（2026-09-19）

| 项 | 内容 |
|---|---|
| **交付物** | `TextProvider` 接口 + `OpenAICompatibleTextProvider`、植物详细分析生成（简介/形态/习性/花期/果期/用途/养护）、识别结果页、置信度与识别质量星级、低置信度提示与补图重新识别 |
| **依赖** | Phase 3 |
| **验收标准** | ① `assembleDebug` 零错误<br>② **文字分析失败时，基础识别结果仍然保存并可查看**（模拟方式：将文字模型 Key 置空后走一遍流程）<br>③ 置信度 < 0.70 时显示补图提示，补图后更新**当前观察**而非新建<br>④ UI 中无「一定正确」「保证鉴定」类表述 |
| **主要风险** | 文字分析失败后的状态管理（须明确 `analysisStatus` 字段，见 Part 5.2） |

#### 实际落地与计划的差异

| 项 | 计划 | 实际 | 原因 |
|---|---|---|---|
| HTTP 层 | 视觉侧独立实现 | 抽出 `ChatCompletionsClient` **两条通道共用** | 端点、12 类错误分类、超时与协程取消逻辑完全一致。各写一份，改一处必漏另一处 |
| 落库时机 | 识别成功即写入 | **先落基础结果，再单独跑文字分析** | 这正是验收标准 ② 的实现方式：文字分析失败只影响描述字段，档案与照片已安全落库 |
| 「补图后更新当前观察」 | Phase 4 完成 | **部分**：Phase 4 的补图发生在保存之前，天然只产生一条观察（已用数据库行数证实）；保存**之后**的补图更新需要观察历史页作为入口，留给 Phase 5 |
| 设置页文字配置 | 独立配置 | 独立配置 + 「使用同一个 AI 服务」开关 | 多数用户会用同一家服务；开关省掉重复填写 |

#### 4.6.1 Phase 4 验证结果

| 组 | 项数 | 结果 |
|---|---|---|
| 前置自检（服务端模式清单 / 设备到模拟服务端连通性） | 2 | ✅ 全通过 |
| 配置前提（落盘确认共用、密钥密文存在、无明文） | 5 | ✅ 全通过 |
| **A：文字分析返回非 JSON** → 档案不受影响 | 13 | ✅ 全通过 |
| **B：文字 Key 为空（验收标准 ②）** → 基础结果仍保存 | 19 | ✅ 全通过 |
| **C：低置信度 → 补图提示（验收标准 ③）** | 6 | ✅ 全通过 |
| 崩溃检查（仅统计本应用进程） | 1 | ✅ 无崩溃 |
| **UI 断言合计** | **46** | **✅ 0 失败** |
| 数据库层校验（表结构 / 关联一致性 / 路径相对性 / 枚举合法性） | 14 | ✅ 全通过 |
| 验收标准 ④：文案核查（「一定正确」「保证鉴定」类表述） | 1 | ✅ 仅出现在**禁止使用它们的注释**里 |
| 编译与 lint | 2 | ✅ `assembleDebug` 成功；lint **0 error** |

**数据库实测的关键证据**：

- 13 份档案、13 条观察、39 张图片 —— **每份档案恰好一条观察**，证实补图重识别没有新建观察
- 无孤儿观察、无孤儿图片、无卡在 `PENDING` 的分析（崩溃或中断会留下这种脏状态）
- 图片路径全部为相对路径，且引用的文件在 `filesDir` 中真实存在
- `analysisStatus` 的三条路径都落到了库里：`SUCCEEDED`（正常）、`FAILED`（场景 A）、`NOT_REQUESTED`（场景 B）

#### 4.6.2 这一轮验证暴露的问题（全部在**验证工具**里，不在应用里）

值得单独记下来：它们都会造成**假通过**，比失败更危险。

| # | 问题 | 表现 | 处理 |
|---|---|---|---|
| 1 | 成功判据不唯一 | 用「已保存」判断设置保存成功，而设置页上本来就有「API Key 已保存」一句 → 保存**根本没发生**时断言也全绿 | 改用 Snackbar 独有的「已加密存放在本机」 |
| 2 | 用界面文字推前提状态 | 靠数屏上出现几个「Base URL」判断配置是否已拆分，被滚动位置左右 | 直接读 DataStore 文件，断言「有 text 字段、无 text 密钥密文」 |
| 3 | **模拟服务端跑的是旧代码** | `/health` 报着 `mode=lowconf`，实际返回默认结果。Python 进程在启动时载入源码，之后改文件对已运行进程毫无影响 | `/health` 一并返回 `modes` 清单；脚本先断言所需模式在清单里；未知模式直接 400 拒绝 |
| 4 | `adb reverse` 映射丢失 | 服务端一切正常，设备却完全连不上 → 识别失败，断言只说「流程未走通」 | 脚本前置检查设备侧可达性，不通则自动重建映射，仍不通则中止 |
| 5 | Compose 的 `Switch` 不是 `Switch` 类 | 按控件类名找不到开关，导致「关闭共用开关」永远失败 | 改按 `checkable="true"` 属性定位，并直接读 `checked` 判断实际状态 |
| 6 | 只看当前视口 | 结果页与详情页都很长，屏幕外的内容被判定为「不存在」 | 断言前先回顶部，再逐屏向下扫到内容不再变化为止 |
| 7 | 辅助进程崩溃算到应用头上 | `uiautomator` 并发调用会报 `UiAutomationService already registered`，被计成应用崩溃 | 只统计 `Process: com.plantidentify` 的崩溃块 |

**结论**：一个「体检」式的验证若自身有缺陷，会给出与事实相反的结论 ——
这和 4.5.2 那次「测试连接误报失败」是同一类错误。
因此验证脚本本身也要有前置自检（服务端版本、设备连通性），
并且**断言必须读硬数据**，不能靠界面文字反推状态。

### 4.7 Phase 5 —— 植物档案与归并

| 项 | 内容 |
|---|---|
| **交付物** | 植物列表（卡片）、详情页、编辑、删除、观察历史、搜索（中/拉丁/科/属/类型/备注）、筛选（科/属/日期/地点）、重复植物归并提示流程 |
| **依赖** | Phase 4 |
| **验收标准** | ① `assembleDebug` 零错误<br>② 同一植物第二次识别触发归并提示；选「添加到已有植物」后观察数 +1，**原观察记录完整保留**<br>③ 选「创建新的植物」后生成新的 `PlantRecord`<br>④ **系统在任何情况下都不自动合并两条已有 `PlantRecord`**<br>⑤ 搜索「紫薇」可同时命中中文名与拉丁学名 |
| **主要风险** | 归并的匹配规则准确度；删除植物时级联删除图片文件（数据库级联不等于文件级联，须显式处理） |

### 4.8 Phase 6 —— 统计、位置、导出、备份

| 项 | 内容 |
|---|---|
| **交付物** | 统计页（三项指标：**不同植物 / 观察次数 / 照片数**，口径已确认）、可选定位与反向地理编码（含降级）、HTML 导出（三级策略）、数据备份与恢复 |
| **依赖** | Phase 5 |
| **验收标准** | ① `assembleDebug` 零错误<br>② **统计页三项指标与数据库行数逐一对应**（造数据验证：N 条 `PlantRecord` / M 条 `PlantObservation` / K 条 `ObservationImage`，页面须分别显示 N / M / K，且**不再出现「植物记录」字样**）<br>③ 导出的 HTML 在电脑浏览器打开，图片正常显示、中文无乱码<br>④ 导出体积在阈值内（默认模式 < 50 MB）<br>⑤ 备份 → 清空数据 → 恢复，档案/观察/图片/备注完全一致<br>⑥ 拒绝定位权限后，全流程仍可正常完成 |
| **主要风险** | **HTML 体积**（Part 1.3 缺口 3）；反向地理编码在国内的可用性（缺口 5）；备份包体积 |
| **范围确认** | ✅ 用户已确认**本 Phase 全做** —— 第一版即包含位置、统计、导出、备份，不放宽到后续版本 |

### 4.9 Phase 7 —— 优化与发布

| 项 | 内容 |
|---|---|
| **交付物** | UI 细节与性能优化、权限处理完善、异常兜底补全、单元测试、Release 签名构建、完整 README |
| **依赖** | Phase 6 |
| **验收标准** | ① `./gradlew assembleRelease` 产出签名 APK<br>② `./gradlew test` 全绿<br>③ README 覆盖规格书第三十五章要求的 8 项说明（导入、运行、配置 API/Base URL/模型、OpenAI Compatible 用法、构建 APK、导出 HTML、备份数据、Key 安全）<br>④ 长列表（100+ 植物）滚动流畅，无掉帧<br>⑤ 大图（50MB+）不导致 OOM |
| **主要风险** | Release 签名配置（keystore 必须 gitignore）；混淆规则与 Compose/Room 的兼容 |

### 4.10 工作量量级 [估算]

不对人日做伪精确估计，仅给相对量级与依据：

| Phase | 工作量量级 | 主要依据 |
|---|---|---|
| 1 工程骨架 | 小 | 模板化程度高，风险在版本对齐 |
| 2 图像采集 | 中 | CameraX 生命周期 + EXIF + 压缩是真实复杂度 |
| 3 视觉识别 | **中大** | 多家 API 兼容 + 容错解析 + 错误分类 |
| 4 文字分析 | 小中 | 结构同 Phase 3，工作量减半 |
| 5 档案与归并 | 中 | CRUD 常规，归并规则需仔细设计 |
| 6 统计/导出/备份 | **中偏大** | HTML 生成与体积控制是主要工作量 |
| 7 优化与发布 | 中 | 测试与异常兜底是长尾 |

**总体判断**：这是一个**中等规模**的 Android 应用。功能面覆盖完整（网络、数据库、相机、文件、位置、导出），但没有算法层面的难点。**真正的风险不在"做不完"，而在 Part 3.4 的卖点验证。**

### 4.11 关于"不要一次性生成整个项目"

规格书第三十四节明确要求：

> 每完成一个 Phase：检查项目是否可以编译 → 检查已有功能是否被破坏 → 修复编译错误 → 修复明显运行时错误 → 再进入下一阶段
> 不要为了追求代码数量而生成大量没有经过验证的代码。

**这条要求应当被严格执行**，它也是本报告与规格书分歧最小的地方。补充两点执行细则：

1. **每个 Phase 结束必须真实执行一次 `assembleDebug`**，而不是"应该能编译"
2. **每个 Phase 开始前先 `git pull`，结束后提交到独立分支**（见 Part 10）

---

## Part 5 数据架构可行性

### 5.1 ER 结构（含修正建议）

规格书第十四节的三表设计是正确的（这正是它区别于"拍照识花"工具的关键）。以下是落地时的字段级修正建议：

```text
PlantRecord（植物档案 —— 一种植物一条）
├── id                Long    PK
├── name              String  中文名称
├── latinName         String? 拉丁学名      ← 归并匹配的第一优先级，须建索引
├── family            String? 科
├── genus             String? 属
├── category          String? 植物类型（落叶灌木/小乔木…）
├── confidence        Double  最近一次识别的置信度
├── description       String? 植物简介
├── morphologicalFeatures String? 形态特征
├── growthHabits      String? 生长习性
├── floweringPeriod   String? 花期
├── fruitingPeriod    String? 果期
├── landscapeUses     String? 园林用途
├── careAdvice        String? 养护建议
├── analysisStatus    Enum    ★新增 NONE/PENDING/OK/FAILED
├── note              String? 用户备注
├── createdAt         Long
└── updatedAt         Long

PlantObservation（观察记录 —— 一次观察一条）
├── id                Long    PK
├── plantId           Long    FK → PlantRecord.id（CASCADE，索引）
├── timestamp         Long    观察时间（索引）
├── latitude          Double? 纬度
├── longitude         Double? 经度
├── locationName      String? ★新增 反向地理编码结果（缓存，避免重复解析）
├── note              String? 本次观察备注
├── aiResultJson      String  原始 AI 返回全文（可追溯）
└── isPrimary         Boolean ★新增 是否为该植物的代表观察

ObservationImage（照片）
├── id                Long    PK
├── observationId     Long    FK → PlantObservation.id（CASCADE，索引）
├── imagePath         String  相对路径 ★（不用绝对路径）
├── role              Enum    ★新增 WHOLE_PLANT/LEAF/FLOWER/FRUIT/BARK/HABITAT/UNKNOWN
└── sortOrder         Int
```

**五处新增字段的理由**：

| 新增 | 理由 |
|---|---|
| `PlantRecord.analysisStatus` | Phase 4 验收标准要求「文字分析失败不丢失识别结果」。没有这个字段，就无法区分「还没分析」和「分析失败了」，也就无法实现「重新生成分析」按钮 |
| `PlantObservation.locationName` | 反向地理编码是有网络成本的异步操作，且在部分机型不可用。缓存结果避免每次渲染列表都重新解析 |
| `PlantObservation.isPrimary` | 植物列表卡片需要一张代表图。没有这个标记就只能取"最新一张"，而用户可能希望固定某张 |
| `ObservationImage.role` | 改进建议 8 的载体，直接对冲 Part 3.4 的多图退化风险 |
| `ObservationImage.imagePath` 存相对路径 | 应用私有目录的绝对路径含随机化沙盒段，换机恢复备份后绝对路径全部失效 |

### 5.2 索引设计

```sql
CREATE INDEX idx_plant_latin      ON PlantRecord(latinName);
CREATE INDEX idx_plant_name_fam   ON PlantRecord(name, family, genus);
CREATE INDEX idx_obs_plant        ON PlantObservation(plantId);
CREATE INDEX idx_obs_timestamp    ON PlantObservation(timestamp);
CREATE INDEX idx_img_obs_sort     ON ObservationImage(observationId, sortOrder);
```

前两个索引直接服务于重复植物归并的匹配查询；后三个服务于列表渲染与观察历史。

### 5.3 重复识别归并的匹配优先级（落地版）

规格书第十四点五节给出的优先级方向正确，此处给出可实现的落地版本：

| 优先级 | 规则 | 判定 | 动作 |
|---|---|---|---|
| 1 | 拉丁学名精确匹配（忽略大小写、去作者名、标准化空格） | 高置信 | 提示「发现已有记录，是否添加为新观察」 |
| 2 | 中文名 + 科 + 属 三者全等 | 高置信 | 同上 |
| 3 | 中文名 + 科 全等 | 中置信 | 提示「可能已存在相同植物，请确认」 |
| 4 | 中文名拼音首字母相同 或 编辑距离 ≤ 2 | 低置信 | 仅提示，**须由用户明确确认** |
| ~~5~~ | ~~AI 语义匹配~~ | — | **第一版不做**（见 Part 1.3 缺口 6） |

**三条铁律**：

1. **系统在任何情况下都不得自动合并两条已有 `PlantRecord`** —— 归并只允许"把新观察挂到已有植物"，不允许"把植物 A 并入植物 B"
2. **不得仅凭中文名做绝对判断** —— 同物异名与同名异物在植物领域都很常见
3. **用户选择「添加到已有植物」时，原有观察记录必须完整保留**，只能追加

### 5.4 Room 迁移策略

| 项 | 要求 |
|---|---|
| `exportSchema` | **必须开启**，schema JSON 提交到版本库（有版本追溯价值，且不含隐私数据） |
| 迁移 | 每个 schema 版本必须提供显式 `Migration`，**禁止 `fallbackToDestructiveMigration()`** —— 用户档案是长期资产，破坏性迁移会清空全部数据 |
| 外键 | `PlantObservation.plantId`、`ObservationImage.observationId` 均设 `onDelete = CASCADE` |
| 文件级联 | 删除植物时，**数据库级联删除不会删除图片文件**，须在 Repository 层显式收集路径并删除 |

最后一条是容易被忽略的坑：Room 的 CASCADE 只作用于数据库行，磁盘上的图片文件需要手动清理，否则会持续占用存储空间形成"孤儿文件"。

---

## Part 6 风险登记册

| ID | 风险 | 概率 | 影响 | 应对 | 触发条件 |
|---|---|---|---|---|---|
| **R1** | **多图联合识别退化，核心卖点失效** | 高 | **高** | 前置验证实验（Part 3.4）；B 组角色标注策略；预案三级（调整表述 / 接入 Pl@ntNet） | 验证实验中 A 组 Top-1 命中率不显著高于单图基线 |
| **R2** | HTML 单文件体积爆炸，导出文件无法打开 | 高 | 中 | 三级导出策略（Part 1.3 缺口 3）；默认嵌缩略图 | 导出体积 > 150 MB 或照片数 > 200 |
| **R3** | 视觉 API 结构化输出不稳定，JSON 解析频繁失败 | 高 | 中 | 容错解析链路 6 步（Part 3.2）；保留原始返回；解析失败降级为半结构化展示 | 解析失败率 > 20% |
| **R4** | 本机无模拟器、无 Android Studio | 高 | 低 | 命令行构建 + 真机 adb；交付物仍是标准 Studio 工程 | 已确认，非风险而是既定条件 |
| **R5** | 磁盘空间不足（C 剩 30 GB） | 中 | 低 | 集中管理构建缓存；定期 `gradlew clean` | 剩余 < 10 GB |
| **R6** | GLM 系多图/base64 支持口径不明，用户配置后必然失败 | 中 | 中 | 首版只承诺 Qwen/豆包/OpenAI 三通道；GLM 标「实验性」；代码注释写明 GLM-4V-Flash 不支持 base64 | 用户反馈 GLM 配置失败 |
| **R7** | 反向地理编码在国内不可用 | 中 | 中 | 系统 Geocoder + 失败降级为经纬度；位置为可选项，不阻塞主流程 | 真机测试中 Geocoder 返回 null 或抛异常 |
| **R8** | API Key 泄漏（仓库转公开时暴露历史） | 中 | **高** | 首次提交前即配好 `.gitignore`；key 永不入库；转公开前做全历史密钥扫描（Part 10.4） | 发现任何提交包含密钥 |
| **R9** | 图片存 cacheDir 被系统静默清理，档案图片全部丢失 | 中 | 中 | **强制使用 `filesDir`**；代码审查重点检查 | 用户反馈图片丢失 |
| **R10** | API 成本失控 | 中 | 低 | 上传前压缩至 1536px；失败不自动多次重试；UI 显示当前配置的模型 | 单次识别费用异常偏高 |
| **R11** | 权限被拒导致流程中断 | 中 | 低 | 相机/位置权限均设计为"拒绝后降级可用"；Photo Picker 无需存储权限 | 拒绝授权后应用崩溃或卡死 |
| **R12** | Room 破坏性迁移清空用户数据 | 低 | **高** | 禁止 `fallbackToDestructiveMigration`；每版本提供显式 Migration | 数据库版本升级后数据丢失 |

**高优先级四项**：R1（卖点）、R2（导出）、R3（解析）、R8（密钥）。其中 **R1 必须在 Phase 3 前解决**，其余可在对应 Phase 内处理。

---

## Part 7 成本与资源

### 7.1 开发成本

| 项 | 成本 |
|---|---|
| 工具链（JDK / Android SDK / Gradle / Compose / Room / CameraX） | **0 元**，全部免费 |
| Android Studio | **0 元**（本机未安装，构建走命令行，不影响交付） |
| GitHub 私有仓库 | **0 元** |
| 视觉/文本 API 调用（开发期测试） | **数元至数十元** [估算] |
| **合计** | **几乎为零** |

这是本项目的显著优势：**零现金成本即可完成全部开发与验证**。

### 7.2 运行成本

单株识别 = 1 次视觉调用 + 1 次文字分析调用。

**视觉调用**（5 张图）[估算]：

```
输入：5 张 × 约 1500 token/图 ≈ 7500 token
输出：结构化 JSON 约 500 token
```

**文字分析调用**：

```
输入：植物名称 + 科属 + prompt ≈ 300 token
输出：植物百科约 1200 token
```

按国产视觉/文本模型的价格量级，**单株识别费用约 0.01–0.1 元** [估算]（各家实际定价差异较大，需以官方价目为准）。

**Pl@ntNet 额度注意**：其免费额度存在**口径冲突** —— 官方 Terms 写 500 次/天，FAQ 写试用期 50 次/天（6 个月）。**集成前须以官网当前条款为准**，不要按乐观数字做容量规划。商业约 3€/1000 次。

### 7.3 仓储与存储

| 项 | 量级 |
|---|---|
| 单株原图（2 张） | 4–8 MB |
| 100 种植物档案 | 400–800 MB |
| 100 种植物备份包 | 400–800 MB（含图片） |

**提示**：档案规模到百种量级时，备份包已达数百 MB，导出与恢复需要进度提示与失败恢复机制。

---

## Part 8 结论与建议

### 8.1 可行性结论

**有条件可行。**

| 维度 | 评级 | 依据 |
|---|---|---|
| 技术可行性 | **高** | 构建链路完整，技术栈全为成熟方案，无算法难点 |
| 数据架构可行性 | **高** | 三表设计正确，字段修正后可支撑全部预期功能 |
| 成本可行性 | **高** | 开发零现金成本，运行费用可控 |
| 进度可行性 | **中高** | 7 个 Phase 划分合理，但需为验证实验留出并行窗口 |
| **核心卖点可行性** | **⚠ 待验证** | 多图联合识别存在实证退化风险（Part 3.4） |

### 8.2 前置条件（3 项已完成 2 项，第 1 项的工具已就绪）

| # | 前置条件 | 状态 |
|---|---|---|
| 1 | **执行多图识别验证实验**（Part 3.4）—— 20–30 组真实样本、几十元成本，确定多图识别是否真的优于单图，并选定 prompt 策略 | 🟡 **待执行 —— 当前唯一未解项**。**实验工具已就绪**：Phase 3 已把 A/B 两种 prompt 策略做成设置页里可切换的配置（并持久化），用户可直接用 App 跑对照；`tools/mock_ai_server.py` 可用于先验证流程。缺的只是**真实 API Key 与 20–30 组同株多图样本** |
| 2 | **澄清术语与统计口径矛盾**（Part 1.3 矛盾 1、2）—— 表名统一 +「植物记录」定义 | ✅ **已完成**（2026-09-18，规格书已修订） |
| 3 | **确认 MVP 边界**（Part 1.2）—— 第一版是否含位置、统计、导出、备份 | ✅ **已完成** —— 确认 Phase 6 全做；第一版不含植物地图、群落调查、PDF、Excel 导出等远期功能 |

**结论：工程侧的前置条件已全部清空，Phase 1–3 已交付。**
唯一未解的仍是验证实验 —— 它现在只缺样本与 Key，不再缺工具。

### 8.2.1 验证实验的具体跑法（Phase 3 之后更新）

Phase 3 的落地使实验成本进一步降低 —— **不需要另写脚本**：

1. 在「设置」页配置一个真实视觉服务（Qwen / 豆包 / OpenAI 任一）
2. 「识别策略」选 **A 组（多图原样）**，添加同一株的 3–5 张照片 → 记录结果
3. 清空照片，用**同一组照片**重新添加，「识别策略」改为 **B 组（部位标注）**
   —— 注意每张照片要标注好部位（叶片/花/果实等），否则 B 组会退化成 A 组的行为
4. 两组结果对比 Top-1 是否一致、置信度分布、是否正确排除近缘种
5. 基准用 Pl@ntNet（它对同一株的多图有官方支持）或人工判定

策略选择会持久化，因此中途退出不会丢设置。切换策略只改 prompt，其余链路完全相同，
差异可以干净地归因到「有没有部位标注」这一件事上。

### 8.3 建议的第一个动作

**先跑验证实验，同时启动 Phase 1。**

两者互不依赖：Phase 1 是纯工程配置（Gradle、Compose、Room），即便卖点结论不理想也不会浪费；验证实验是纯脚本，不碰 Android 代码。等 Phase 1/2 做完，实验结论也已到手，正好进入 Phase 3。

**这是本报告最重要的一条建议：不要把验证推到 Phase 3 之后。**

### 8.4 明确不建议做的事

| 不建议 | 理由 |
|---|---|
| 把 confidence 宣传为科学鉴定概率 | 规格书第三十一节已禁止；且验证实验会检验置信度与实际正确性的相关性，若不相关则展示本身具有误导性 |
| 第一版实现 AI 语义匹配 | 成本、延迟、不确定性都不划算，规则匹配已足够（Part 1.3 缺口 6） |
| 第一版实现植物地图、群落调查、PDF、Excel 导出 | 远期功能，会显著拉长周期且不解决核心问题 |
| 为每家 AI 服务商写独立的 Provider 类 | 它们都提供 OpenAI 兼容端点，差异应抽成配置而非继承（Part 2.4） |
| 使用 `EncryptedSharedPreferences` | 已被官方弃用，应使用 DataStore + Keystore |
| 使用 `fallbackToDestructiveMigration()` | 会清空用户长期档案 |
| 把图片存进 `cacheDir` | 系统会静默清理，导致档案图片丢失 |
| 为追求代码数量生成未验证的代码 | 规格书第三十四节已明确禁止，务必执行 |

### 8.5 对规格书的整体评价

规格书是一份**质量相当高**的需求文档，值得肯定的是：

- **产品定位清醒**：明确区分「拍照识花」与「多图联合识别 + 数字档案」，没有落入同质化
- **架构判断正确**：视觉与文本 Provider 分离、OpenAI Compatible 统一、三表数据结构 —— 这几处都是正确的技术决策
- **有产品伦理意识**：主动要求不夸大 AI 能力，这在同类文档中少见
- **工程纪律明确**：分 Phase 递进、每步验证、禁止生成未验证代码

需要补强的是：**它把一个未经工程验证的技术假设（多图联合识别可行）当作了既定的产品前提**。本报告的核心价值，就是把这个隐含假设显式化，并给出低成本的验证路径。

---

## Part 9 决策记录（5 项已确认）

**全部 5 项已于 2026-09-18 由用户确认，并已落地到规格书与相关章节。**

| # | 决策项 | ✅ 确认结论 | 落地位置 |
|---|---|---|---|
| 1 | **表名与类名** | 统一为 `PlantRecord` / `PlantObservation` / `ObservationImage`；页面为 `AddPlantScreen` / `PlantDetailScreen` | 规格书全文 20 处已修订 + 文首「修订记录」；分析见 Part 1.3 矛盾 1 |
| 2 | **统计口径** | **弃用「植物记录」**，统一为三项：不同植物（`PlantRecord` 数）/ 观察次数（`PlantObservation` 数）/ 照片数（`ObservationImage` 数）。原「植物记录 127」作废 | 规格书第十五、二十、二十一节已修订；Phase 6 验收标准已加对应断言（Part 4.8） |
| 3 | **MVP 边界** | **含位置 + 统计 + HTML 导出 + 数据备份**，Phase 6 全做，不放宽 | Part 1.2 增强层、Part 4.8 已标注确认 |
| 4 | **Pl@ntNet** | **先仅用作验证实验的对照基准**；是否常驻为兜底通道**等实验结果再定** | Part 3.4 实验设计、Part 3.5 |
| 5 | **首版视觉通道** | **预置 Qwen / 豆包 / OpenAI 三家默认配置**（base_url + 模型名），同时保留自定义 OpenAI Compatible 入口 | 待 Phase 3 实现（Part 4.5） |

### 9.1 由决策衍生的新要求

1. **统计三项并列展示时须标注语义差异** —— 「不同植物」是去重物种数、「观察次数」是累计记录数、「照片数」是文件数，分母不同，UI 上不能让用户误读为同一维度的计数
2. **Phase 3 配置页预置三家默认值，但不得硬编码** —— 默认值只是表单初值，用户仍可完全覆盖（规格书第十节要求「不要把服务商名称硬编码成不可扩展的逻辑」）
3. **三家的差异抽成配置而非独立类** —— base_url、模型名、多图上限、base64 是否需去掉 data URI 前缀（GLM 系尤其）都应落在配置文件，而不是写三份 Provider 实现
4. **Pl@ntNet 的集成决策被显式推迟** —— 在实验结果出来前不要为它预写实现代码，只需保证 `VisionProvider` 接口可替换

### 9.2 无遗留决策项

原「待确认决策清单」已全部关闭。开工前不再需要任何用户拍板，**唯一待办是把验证实验跑出来**。

---

## Part 10 工程协作约定

以下规范已确立，后续开发直接遵循。

### 10.1 仓库策略：先私有，验证后公开

| 阶段 | 状态 | 触发条件 |
|---|---|---|
| 当前 | **私有** `Battlemagnet1/plant-identify` | — |
| 将来 | 转公开 | 用户人工验证功能完整性通过 → 抹除 API Key → 全历史密钥扫描通过 |

**关键事实**：仓库从私有改为公开时，**整个提交历史**会一并公开。后来删掉密钥的那次提交并不能保护它。因此从第一次提交起就按公开标准执行。

### 10.2 分支与合并

- `main` 为稳定分支，**始终保持可编译**
- 每个 Phase 在独立分支开发：

| Phase | 分支名 |
|---|---|
| 1 | `feature/phase1-project-skeleton` |
| 2 | `feature/phase2-camera-gallery` |
| 3 | `feature/phase3-vision-provider` |
| 4 | `feature/phase4-text-provider` |
| 5 | `feature/phase5-plant-archive` |
| 6 | `feature/phase6-stats-and-export` |
| 7 | `feature/phase7-polish-and-release` |

- 变更通过 Pull Request 提交，**经人工审阅确认后**以 **squash** 方式合并并删除分支

### 10.3 标准流程

```bash
# 1. 从最新的 main 切出分支
git checkout main && git pull --ff-only
git checkout -b feature/phaseN-<slug>

# 2. 实现后自检（JAVA_HOME 必须显式指定）
JAVA_HOME=C:/Users/a/jdk/jdk17.0.20_8 ./gradlew assembleDebug

# 3. 提交
git add -A
git status && git diff --cached --stat   # 确认无密钥文件
git commit -m "feat(phaseN): <做了什么>"

# 4. 推送并开 PR
git push -u origin feature/phaseN-<slug>
gh pr create --base main

# 5. 人工审阅确认后合并
gh pr merge <编号> --squash --delete-branch
git checkout main && git pull --ff-only
```

### 10.4 安全红线

1. **绝不提交** API Key：真实 key 只放 `local.properties`（已 gitignore）或 App 内 Keystore 加密存储
2. **绝不提交**签名 keystore 与 `keystore.properties`
3. 文档、README、代码注释中的示例 key 一律用占位符（`sk-xxxx`）
4. 每次 `git add` 前检查暂存区，确认没有 `local.properties`、`.env`、`*.jks`
5. 误提交的补救：**① 先到服务商平台吊销并重新生成 key（不可省，清理历史不能阻止已被抓取）→ ② 用 `git filter-repo` 重写历史 → ③ 强推**

### 10.5 转公开前置清单

待用户人工验证功能完整性后，逐项核对（全部通过才可转公开）：

- [ ] 全历史密钥扫描：`git log --all -p` 或 `gitleaks detect`，确认无任何 key / keystore / `.env` 残留
- [ ] `git log --all --full-history -- local.properties` 无输出
- [ ] 仓库内无任何真实可用的第三方 API Key（含报告、README、示例配置、测试代码）
- [ ] 文档中无个人隐私信息（邮箱、手机号等）
- [ ] `.workbuddy/` 确认未被跟踪

通过后执行：

```bash
gh repo edit Battlemagnet1/plant-identify --visibility public --accept-visibility-change-consequences
```

### 10.6 命名与统计口径约定（2026-09-18 确认）

**命名**（规格书已同步修订，实现时不得回退到旧名）：

| 类型 | 命名 |
|---|---|
| Room 实体 / 数据类 | `PlantRecord`、`PlantObservation`、`ObservationImage` |
| 外键字段 | `PlantObservation.plantId`、`ObservationImage.observationId` |
| Compose 页面 | `AddPlantScreen`、`PlantDetailScreen`、`ObservationScreen`、`SettingsScreen` |
| Provider | `VisionProvider`、`TextProvider`（接口）；`OpenAICompatibleVisionProvider`、`OpenAICompatibleTextProvider`（唯一实现） |

**统计口径**（三项指标，禁止再使用「植物记录」这一表述）：

| 指标 | 定义 |
|---|---|
| 不同植物 | `PlantRecord` 行数 |
| 观察次数 | `PlantObservation` 行数 |
| 照片数 | `ObservationImage` 行数 |

UI 中三项须并列展示并标注语义差异（去重物种数 / 累计记录数 / 图片文件数），不可让用户误读为同一维度。

**Python / Kotlin 通用**：本仓库所有标识符一律使用 `Plant`（不是 `plantt`）；`plantId` 不是 `planttId`。

---

## 附录 A 数据来源

本报告的关键事实来源：

**构建工具链**
- Android Gradle Plugin 版本与 Gradle 兼容矩阵 — developer.android.com AGP release notes
- Kotlin 版本与 Compose 编译器插件 — kotlinlang.org / Compose 编译器文档
- Compose BOM 版本映射 — developer.android.com Compose BOM mapping
- targetSdk 要求 — developer.android.com target-sdk 政策页
- 权限与媒体访问（Photo Picker / READ_MEDIA_IMAGES）— developer.android.com

**视觉模型能力**
- Qwen-VL 多图与 base64 限制 — docs.qwencloud.com developer-guides/multimodal/vision
- Qwen 结构化输出支持范围 — docs.qwencloud.com developer-guides/text-generation/structured-output
- 豆包视觉模型参数与限制 — volcengine.com 火山方舟文档
- GLM-4V 限制（来源冲突，标注为不确定）— 二手社区来源，官方文档页 404

**多图退化证据**
- Chapa 等，多图 VLM 失败模式的受控分析
- 多图 VLM 跨图综合基准评测（InternVL3.5 / Phi-4-multimodal / Qwen2.5-VL 得分）

**替代方案**
- Pl@ntNet API 条款与配额 — my.plantnet.org/terms_of_use、api.plantnet.org FAQ
- iNaturalist CV API 现状 — forum.inaturalist.org 讨论、github.com/inaturalist/inatVisionAPI

**本机环境**
- 全部为本次会话直接探测所得（[实测] 标注项）

---

## 附录 B 待办清单

- [x] ~~澄清 Part 9 的 5 项决策~~ —— ✅ 2026-09-18 已全部确认
- [x] ~~修订规格书的表名与统计口径~~ —— ✅ 已完成（20 处标识符 + 3 处统计模板）
- [ ] **准备 20–30 组同株多图测试样本（含 3–5 组易混种）** ← 当前待办
- [ ] **执行多图识别验证实验（A/B/C 三组对照）** ← 当前待办，唯一前置条件
- [ ] 根据实验结论确定 Phase 3 的 prompt 策略，并决定是否常驻 Pl@ntNet
- [ ] 启动 Phase 1 工程骨架（可与上面的实验并行）

### 附录 B.1 修订影响清单（供 Phase 1 开工时对照）

修订后，以下位置在实现时必须使用新名称与新区口径：

| 位置 | 旧 | 新 |
|---|---|---|
| Room 实体 | `planttRecord` | `PlantRecord` |
| Room 实体 | `planttObservation` | `PlantObservation` |
| 外键字段 | `planttId` | `plantId` |
| Compose 页面 | `AddplanttScreen` | `AddPlantScreen` |
| Compose 页面 | `planttDetailScreen` | `PlantDetailScreen` |
| 统计指标 | 「植物记录」 | 不同植物 / 观察次数 / 照片数 |
