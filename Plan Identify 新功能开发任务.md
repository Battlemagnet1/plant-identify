# Plan Identify 新功能开发任务

当前 Plan Identify 已经基本开发完成。现在需要在**不破坏现有功能和数据库结构的前提下**，增加两个核心功能：

1. **植物识别任务队列**
2. **植物数据清洗与智能去重**

请先分析当前项目代码、数据库、页面导航和 AI 调用架构，再进行开发。不要重新搭建整个项目。

------

# 一、植物识别任务队列

## 1. 功能目标

针对景观现场一次需要拍摄大量植物的场景，实现：

> **连续拍摄 → 暂存任务 → 后续统一识别**

用户不需要每拍一株植物就等待 AI 返回结果。

例如：

```text
现场拍摄
 ↓
植物 A → 加入任务
植物 B → 加入任务
植物 C → 加入任务
……
 ↓
任务列表
 ↓
用户可以逐个识别或全部识别
 ↓
后台执行 AI
 ↓
生成 PlantObservation / PlantRecord
```

------

## 2. RecognitionTask 数据结构

增加独立的识别任务实体，不要直接把任务当成 PlantRecord。

建议包含：

```text
RecognitionTask
- id
- status
- imagePaths
- createdAt
- startedAt
- completedAt
- retryCount
- errorMessage
- resultObservationId
- priority
```

任务状态至少支持：

```text
PENDING       待识别
QUEUED        排队中
PROCESSING    识别中
ANALYZING     分析中
COMPLETED     已完成
FAILED        失败
CANCELLED     已取消
```

------

## 3. 任务创建

用户拍摄植物时增加：

> 加入识别任务

而不是强制立即调用 AI。

任务支持：

- CameraX 拍照
- Photo Picker 添加照片
- 1～5 张照片
- 删除照片
- 调整照片顺序
- 查看照片
- 删除任务

同一个任务中的照片属于同一株/同一种植物，应进行**多图联合识别**。

------

## 4. 任务列表页面

新增：

> **识别任务**

页面显示：

```text
全部任务
待识别
识别中
已完成
失败
```

每个任务显示：

- 缩略图
- 照片数量
- 创建时间
- 当前状态
- 错误信息
- 操作按钮

例如：

```text
🌳 植物任务 #018
4 张照片
创建于 2026-09-20

待识别

[识别]
```

识别中：

```text
🌿 植物任务 #019
3 张照片

识别中...
```

失败：

```text
🌸 植物任务 #020
2 张照片

识别失败
网络连接超时

[重试]
```

------

## 5. 单任务识别

每个任务提供：

> **识别**

点击后：

```text
PENDING
 ↓
QUEUED
 ↓
PROCESSING
 ↓
ANALYZING
 ↓
COMPLETED
```

完成后：

```text
RecognitionTask
      ↓
PlantObservation
      ↓
匹配已有 PlantRecord
      ↓
新建植物 / 添加到已有植物
```

不要重复创建 PlantRecord。

------

# 二、后台任务执行

不要通过简单 Thread 实现后台任务。

Android 使用：

> **WorkManager**

实现任务队列。

要求：

- App 切到后台后任务仍可继续
- 支持锁屏后的合理后台执行
- App 重启后能够恢复未完成任务
- 网络错误自动重试
- API 超时自动重试
- 设置最大重试次数
- 失败任务可以手动重试
- 用户可以取消尚未执行的任务

对于需要网络的 AI 任务，应正确设置 WorkManager 的网络约束。

不要保证 Android 在任何情况下都能无限后台运行，应遵循 Android 系统后台执行限制。

------

# 三、批量识别

任务列表顶部提供：

> **全部识别**

例如：

```text
待识别：17
识别中：2
已完成：31

[全部识别]
```

点击后将 PENDING 任务加入 WorkManager 队列。

不要同时无限并发调用 API。

提供合理的并发限制，例如：

```text
最大并发任务数：1～3
```

具体数值根据项目现有实现选择。

避免：

- API 请求爆炸
- 手机内存占用过高
- API 限流
- 多任务同时修改数据库导致冲突

------

# 四、现场快速调查模式

如果当前项目结构允许，增加：

> **现场调查模式**

目标：

让用户在景观现场快速连续拍摄植物。

流程：

```text
进入现场调查
 ↓
拍摄植物
 ↓
自动生成任务 #001
 ↓
继续拍摄
 ↓
自动生成任务 #002
 ↓
继续拍摄
……
```

现场不要求用户立即填写植物名称。

界面显示：

```text
今日调查

已拍摄：23 株
待识别：17 株
识别中：2 株
已完成：4 株

[📷 快速拍摄]

最近任务
```

任务可以使用：

```text
P-001
P-002
P-003
```

作为临时编号。

识别完成后再转换成正式植物档案。

------

# 五、植物数据清洗

新增：

> **数据清洗 / 数据质量中心**

目标：

对现有 PlantRecord、PlantObservation 和图片进行检查。

核心原则：

> **本地算法负责大量初筛，AI 只负责难以通过规则判断的情况。**

不要让 AI 对所有植物进行两两比较。

------

# 六、数据清洗流程

整体流程：

```text
全部植物数据
      ↓
本地规则检查
      ↓
本地相似度算法
      ↓
生成疑似异常/重复候选
      ↓
AI 判断复杂情况
      ↓
生成清洗报告
      ↓
用户确认
      ↓
合并 / 删除 / 修复 / 忽略
```

------

# 七、本地规则清洗

以下情况优先使用本地算法，不调用 AI：

### 数据完整性

检查：

- 中文名为空
- 拉丁名为空
- 科为空
- 属为空
- Observation 不存在
- Observation 图片不存在
- 图片路径失效
- 图片文件损坏
- 数据库外键异常

### 数据格式

检查：

- 时间异常
- 经纬度越界
- confidence 超出 0～1
- 非法 JSON
- 不符合预期的植物类型

### 重复图片

使用：

- SHA-256
- 图片 Hash

检测完全相同的图片。

不要仅依靠文件名判断图片重复。

------

# 八、疑似重复植物检测

不要进行全数据库 O(N²) 暴力 AI 比较。

使用本地算法建立候选集合。

匹配优先级：

```text
1. 拉丁学名完全相同
2. 中文名 + 科 + 属
3. 中文名相似度
4. 拉丁学名相似度
5. 科属信息
6. 植物描述相似度
```

可以使用：

- 编辑距离
- Jaccard Similarity
- Token 相似度
- SimHash
- 本地字符串归一化

根据项目实际情况选择合理方案。

------

# 九、AI 智能判断

只有本地算法发现“可能存在问题”时，才调用 AI。

AI 负责：

### 1. 判断是否可能是同一种植物

例如：

```text
记录 A：
中文名：悬铃木
拉丁名：Platanus × acerifolia

记录 B：
中文名：法国梧桐
拉丁名：Platanus × acerifolia
```

AI 判断：

```text
is_same: true
confidence: 0.98
reason: "拉丁学名一致，中文名称属于常见名称差异"
```

------

### 2. 判断名称是否存在别名关系

例如：

```text
木棉
红棉
英雄树
```

但不要让 AI 因为名称相似就直接判断为同一种。

必须结合：

- 学名
- 科
- 属
- 植物描述
- 其他已有信息

------

### 3. 检查字段冲突

例如：

```text
中文名：桂花
拉丁名：Rosa chinensis
科：蔷薇科
属：木犀属
```

AI 应识别：

```text
存在明显信息冲突
```

并给出原因。

------

# 十、AI 清洗必须结构化输出

不要直接返回自然语言。

例如：

```json
{
  "results": [
    {
      "group_id": "001",
      "type": "POSSIBLE_DUPLICATE",
      "record_ids": ["12", "38"],
      "is_same": true,
      "confidence": 0.96,
      "reason": "拉丁学名一致，中文名称不同但属于常见名称差异"
    },
    {
      "group_id": "002",
      "type": "DATA_CONFLICT",
      "record_ids": ["72"],
      "confidence": 0.94,
      "reason": "中文名与拉丁学名明显不匹配"
    }
  ]
}
```

------

# 十一、AI 批量处理

不要每个候选组单独调用一次 AI。

将多个候选组批量发送：

```text
候选 1
候选 2
候选 3
……
候选 20
```

让 AI 一次返回多个 JSON 结果。

控制单次请求的数据量，避免 Prompt 过长。

------

# 十二、清洗中心 UI

增加：

> **数据清洗**

首页显示：

```text
数据库健康度

████████░░ 86%

共 327 条植物记录

✓ 正常：306
⚠ 疑似重复：12 组
⚠ 信息冲突：5 条
⚠ 缺失字段：4 条
⚠ 图片异常：2 条

[开始检查]
```

------

# 十三、清洗结果

例如：

```text
疑似重复

①

悬铃木
法国梧桐

相似度：96%
AI 判断：可能为同一种植物

[查看详情]
[合并]
[忽略]
```

信息异常：

```text
⚠ 植物信息冲突

桂花

中文名：桂花
拉丁名：Rosa chinensis

AI 判断：
中文名与拉丁学名疑似不匹配。

[重新识别]
[编辑]
[忽略]
```

------

# 十四、植物记录合并

绝对不要简单地：

```text
A + B → 删除 B
```

合并时：

```text
PlantRecord A
+
PlantRecord B
↓
选择主记录
↓
合并植物信息
↓
合并 Observation
↓
合并 Image
↓
保留历史数据
```

例如：

```text
悬铃木
├── Observation 1
├── Observation 2
├── Observation 3
├── Observation 4
└── Observation 5
```

原来的观察记录不能因为合并而丢失。

------

# 十五、字段冲突处理

如果两个 PlantRecord 的字段不同：

例如：

```text
A：
简介 = XXX

B：
简介 = YYY
```

不要直接覆盖。

应该：

1. 优先保留可信度更高的数据
2. AI 可以提供建议
3. 必要时让用户选择
4. 合并前展示最终结果预览

例如：

```text
合并预览

植物名称：
悬铃木

拉丁学名：
Platanus × acerifolia

科：
悬铃木科

观察记录：
A：3 条
B：5 条

照片：
A：8 张
B：12 张

[确认合并]
[取消]
```

------

# 十六、删除机制

数据清洗中的删除必须谨慎。

对于疑似异常数据：

> 默认不要直接删除。

提供：

- 忽略
- 修复
- 重新识别
- 移入回收站

如果实现删除功能，建议使用软删除：

```text
deleted = true
```

而不是直接物理删除。

提供恢复机制。

------

# 十七、数据清洗成本控制

必须优先采用：

> **本地算法 → 候选筛选 → AI**

而不是：

> **全部数据 → AI**

要求：

- 正常数据不调用 AI
- 相同拉丁学名优先本地判断
- 相同图片优先 Hash 判断
- 空字段、格式错误本地处理
- 只有疑难重复和语义冲突调用 AI
- AI 支持批量请求
- 不要重复检查已经确认过的数据

可以保存清洗结果：

```text
lastCheckedAt
cleaningVersion
```

后续只检查新增或发生变化的记录。

------

# 十八、增量清洗

新增植物后，不需要重新扫描整个数据库。

流程：

```text
新增 PlantRecord
      ↓
本地候选匹配
      ↓
发现疑似重复
      ↓
AI 判断
```

只有用户主动点击：

> 全库深度检查

时才扫描整个数据库。

------

# 十九、与现有 AI 植物识别系统的关系

最终整体架构：

```text
                  植物照片
                     ↓
             RecognitionTask
                     ↓
               VisionProvider
                     ↓
                TextProvider
                     ↓
             PlantObservation
                     ↓
             PlantRecord 匹配
                ↙         ↘
          已有植物         新植物
             ↓               ↓
        添加 Observation   创建 Record
             ↓
       数据质量检查/清洗
             ↓
     ┌───────┴────────┐
     ↓                ↓
  重复检测          异常检测
     ↓                ↓
  AI判断            AI判断
     ↓                ↓
     用户确认 / 修复 / 合并
```

------

# 二十、开发要求

在开始修改代码之前：

1. 阅读当前项目结构
2. 阅读现有 Room Entity
3. 阅读 Repository
4. 阅读 AI Provider
5. 阅读 Navigation
6. 阅读现有植物详情页
7. 判断哪些代码可以复用

不要重复实现已有功能。

优先复用：

- 现有图片选择
- 现有 AI Provider
- 现有 Room 数据库
- 现有 PlantRecord
- 现有 PlantObservation
- 现有 UI 组件

------

# 二十一、开发顺序

## Phase 1：修复问题

先实现：

- 1.植物名称不统一，有时候用俗称商品名有时候用正式名字，请在提示词写好了，俗称和正式名字要分开
- 2.定位稀烂，最多只能定位到区，请让定位更加精确，具体定位到地点名，而且可以通过定位来确定植物的地理位置更加精确的判断植物的信息
- 3.AI跑的实在是太慢了，说十几秒结果要两三分钟，你看怎么优化这个速度
- 4.完整版的左上角不需要写完整版三个字，两行很难看

确认可以正常运行后再继续。

## Phase 2：**植物识别任务队列**

- 完成上面一到四的功能，按照上面要求写

## Phase 3：**植物数据清洗与智能去重**

- 完成上面五到十九的功能，按照上面要求写

## Phase 4：优化

测试：

- 100 条
- 1000 条
- 10000 条植物记录

重点检查：

- 数据库查询速度
- UI 流畅度
- WorkManager 稳定性
- AI 请求数量
- 内存占用
- 图片存储
- 大量任务时的任务队列表现
- 发布release，版本为1.0.1，仍然是full版本，base版本不动

------

# 二十二、重要原则

1. **不要破坏现有功能**
2. **不要重复创建已有 PlantRecord**
3. **RecognitionTask 与 PlantObservation 分离**
4. **重复植物与重复观察分离**
5. **AI 不负责所有数据清洗**
6. **优先本地算法**
7. **AI 只处理复杂语义问题**
8. **不要未经用户确认自动合并植物**
9. **不要直接物理删除重要数据**
10. **所有清洗操作尽量可撤销**
11. **AI 返回必须结构化**
12. **后台任务必须考虑 Android 生命周期**
13. **大数据量下避免 O(N²) 全量比较**
14. **支持增量数据清洗**

开始开发前，请先输出：

- 当前项目结构分析
- 现有数据库结构
- 新增 Entity/DAO/Repository
- RecognitionTask 与现有数据的关系
- WorkManager 工作流
- 数据清洗算法流程
- AI 清洗接口设计
- 页面与 Navigation 修改方案
- 需要修改/新增的文件列表

**先不要直接生成全部代码。**

